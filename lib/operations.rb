def unsafe?(speed)
	if ((speed < 40) or (speed >60))
		return true
	else
		return false
end
end


def not_safe?(speed)
	if speed < 40 ? return true : false
  if speed > 60 ? return true : false
end
end
end
